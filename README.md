# Product Store

A full-stack product management app built with React, Node.js, Express, and MongoDB.

## Setup

### Backend
cd backend
npm install
# create a .env file with: MONGO_URI=your_mongodb_uri
npm run dev

### Frontend
cd frontend
npm install
npm run dev
